# dependencies
/node_modules
server/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

#notes
notes.txt

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma generated output
prisma/generated/
prisma/generated

# playwright test output
playwright-report/
test-results/

# planning (GSD workflow artifacts)
.planning/

# raw face-bearing source media (JJ's uncut video). The derived, cropped clips under
# public/private-games/ are what ship, and the mirror strips those. Committing a raw
# source here would put a face in git history, where export-public.sh cannot remove it.
assets/videos/
